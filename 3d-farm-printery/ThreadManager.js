const Printer = require('./api/models/printer');
const Thread = require('./Thread');

class ThreadManager {
    constructor() {
        this._pool = [];
    }

    _init() {

    }
}

module.exports = ThreadManager;